# Prof. Dr. Zekiye Çınar's Website

This website is built with Hugo (static site generator).

## Development

To start the local development server:

```bash
$ hugo server
```

The site will be available at `http://localhost:1313/cinarz/`.

## Build

To generate the static production files (output in `public/`):

```bash
$ hugo
```
